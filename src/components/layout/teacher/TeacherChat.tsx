'use client';

import React from 'react';
import AdminChat from '../admin/AdminChat';

// Teacher chat uses the same component since the chat UI is identical
export default function TeacherChat() {
  return <AdminChat />;
}
