/* Fees Note — public surface. DashboardLayout mounts this for the
   'principal-note' page key; nothing else in the folder is imported outside. */

export { default } from './PrincipalFeesNote';
export { default as PrincipalFeesNote } from './PrincipalFeesNote';
